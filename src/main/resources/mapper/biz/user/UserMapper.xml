<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.heyso.SeedBEApp.biz.user.dao.UserMapper">
    <resultMap id="UserAccountMap" type="com.heyso.SeedBEApp.biz.user.model.UserAccount">
        <id property="userId" column="USER_ID" />
        <result property="username" column="USERNAME" />
        <result property="passwordHash" column="PASSWORD_HASH" />
        <result property="name" column="NAME" />
        <result property="email" column="EMAIL" />
        <result property="useYn" column="USE_YN" />
    </resultMap>

    <select id="findByUsername" parameterType="string" resultMap="UserAccountMap">
        SELECT USER_ID, USERNAME, PASSWORD_HASH, NAME, EMAIL, USE_YN
        FROM TB_COM_USER
        WHERE USERNAME = #{username}
    </select>

    <select id="findRoles" parameterType="long" resultType="string">
        SELECT r.ROLE_KEY
        FROM TB_COM_USER_ROLE ur
        JOIN TB_COM_ROLE r ON r.ROLE_ID = ur.ROLE_ID
        WHERE ur.USER_ID = #{userId}
    </select>
</mapper>